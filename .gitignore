# macOS
.DS_Store

# Xcode
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.swiftpm/
.build/

# Note: Finna.xcodeproj is generated by xcodegen (project.yml) but is committed
# so the client can open the project directly in Xcode without xcodegen installed.
